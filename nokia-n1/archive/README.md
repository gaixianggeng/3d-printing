# 历史方案

`v3/` 保存早期连续面板设计和当时的切片。`openscad/` 保存更早的参数化方案，`docs/` 保存其设计说明。这些文件不作为当前打印入口，不要与 V4 混装。

历史文档中的 `build/`、`3D打印/Builder/` 等导出路径是旧工作区记录，不是本仓库现有路径。未归档这些历史导出目录。当前模型和打印盘请从 [Nokia 项目入口](../README.md) 进入。
