# 协作约定

- 本仓库存放 3D 打印项目，每个模型使用独立目录，文档使用中文。
- 保留可编辑源文件和毫米单位的 STL。修改模型后再更新对应切片，标明目标打印机、喷嘴和耗材。
- 不把切片成功写成实物验证。分别记录模型检查、切片检查、实际打印和试装结果。
- 不混用 Nokia V3 与 V4 零件。archive 只作历史参考。
- Nokia V4 的整块正面必须沿用原打印方向，不自动平放或缩放。
- 不未经要求发送打印任务，不提交凭据、临时缓存、自动备份或无关 App 代码。
- 归档材料校验命令见 nokia-n1/README.md。修改几何后另做相应网格与切片检查。
