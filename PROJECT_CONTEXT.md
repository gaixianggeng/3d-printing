# 仓库上下文

更新日期：2026-09-08。基于已确认的模型工作整理，不是聊天全文。

## 目标

建立长期保存多个 3D 打印项目的独立 GitHub 仓库。首个项目为 Nokia N1 桌面外壳。

## 方案

每个项目单独存放。Nokia 当前版本位于 `nokia-n1/v4/`，早期 Blender V3 与 OpenSCAD 方案位于 `nokia-n1/archive/`。原 Android 工作区保留原文件，本仓库保存复制件，后续在本仓库维护模型。

用户已确认主体打印完成且试装没有问题。最近选择的是细端 2.6 mm 的短销，共 8 根；已排盘、切片并在拓竹中打开，但尚未收到这批销的实际打印或松紧反馈。

## 实现

模型使用 Blender，切片使用 Bambu Studio 的 P2S、0.4 mm 喷嘴和 PLA 设置。归档包含可编辑场景、建模/验证脚本、STL、3MF、预览及检查结果。3MF 内已有切片路径，不重复保存独立 G-code。没有运行时服务或密钥配置。

初始化时比较了复制前后模型、STL、3MF、图片和 PDF 的 SHA-256。只调整复制后脚本的输出目录，使其跟随脚本所在位置；未重新生成几何。旧文档作为历史记录保留，当前打印入口以 Nokia README 为准。

## 风险与待确认

塑料销的松紧、夹持力和后支架承载尚未实物验证。8 根短销不是 12 个不同安装位置的完整紧固件套装。Bambu Studio 可能在重新打开工程时套用本机预设，重新切片前必须核对参数。

下次继续 Nokia 项目时，先记录用户的插销实测结果，再决定是否改直径或生成不同长度的整套销。当前仓库整理不依赖该反馈。

## EarPods 材料补充

`earpods-microphone-stand/` 已归档指定任务的 7 张概念图、4 份历史提示词，以及从最终回复提取的固定圆盘方案提示词。用户最后确认取消旋转支架，采用底座、短支柱和固定圆盘。该项目尚无制造模型、切片或实物验证。
